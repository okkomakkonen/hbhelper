# hbhelper

This CLI tool can be used to import CSV files to [HomeBank](https://www.gethomebank.org/en/). Install locally by running

```bash
uv tool install .
```

in this repository (you may need to include the `--reinstall` flag).
